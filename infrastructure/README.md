# Creator Analytics – Infrastructure

Dieses Verzeichnis enthält alle Dateien zur lokalen Entwicklung mit Docker Compose.

## Setup

1. Docker + Docker Compose installieren
2. Projekt im Terminal öffnen
3. Backend & Frontend Container starten:

```bash
docker-compose up --build
